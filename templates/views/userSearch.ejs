<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  <%- include('../partials/ProfileHeader') %>
    <h1 class="mt-5 pt-5">search results</h1>
    
<ul>
    <% blogs.forEach(blog => { %>
      <li><a href="/blogs/<%= blog._id %>"><%= blog.title %></a></li>
    <% }); %>
  </ul>

</body>
</html>